SELECT cust, prod, sum(X.quant), max(X.quant), sum(Y.quant), sum(Z.quant), count(Z.quant)
FROM sales
GROUP BY cust, prod: X, Y, Z
SUCHTHAT X.state = 'NY', Y.state = 'NJ', Z.state = 'CT'
HAVING max(X.quant) > max(Y.quant)